[project]
name = "epsbasin"
version = "0.2.0"
description = "Tools for computing and visualizing ε-attracting basin"
authors = [
    {name = "Yusuke Imoto",email = "imoto.yusuke.4e@kyoto-u.ac.jp"}
]
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "numpy (>=1.20)",
    "pandas (>=1.2)",
    "scipy (>=1.6)",
    "scikit-learn (>=1.0)",
    "matplotlib (>=3.4)",
    "anndata (>=0.11.4)"
]
license = "MIT"

[project.urls]
repository = "https://github.com/yusuke-imoto-lab/eps-attracting-basin"

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

